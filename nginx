docker run --rm --entrypoint=cat nginx /etc/nginx/nginx.conf > ./nginx.conf
docker run --rm --entrypoint=cat nginx /etc/nginx/conf.d/default.conf > ./default.conf
docker run -d -p 80:80 -v ./html:/usr/share/nginx/html -v ./default.conf:/etc/nginx/conf.d/default.conf nginx